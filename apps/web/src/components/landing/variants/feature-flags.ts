/**
 * Hardcoded feature flags for landing page variants.
 * Set a variant to `true` to show it in the switcher.
 */
export const ENABLED_VARIANTS: Record<string, boolean> = {
  terminal: true,
  glass: false,
  neobrutal: false,
  swiss: false,
  cyberpunk: false,
  bento: false,
  story: false,
  mesh: false,
  retro: false,
  editorial: false,
};
