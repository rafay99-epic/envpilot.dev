/**
 * Compat barrel — preserves the public `api.favorites.*` paths.
 * Implementation lives in features/projects/favorites.ts.
 */
export { listByUser, toggle } from "./features/projects/favorites";
