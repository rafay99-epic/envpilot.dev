/**
 * Compat barrel — preserves the public `api.supportTickets.*` paths.
 * Implementation lives in features/support/supportTickets.ts.
 */
export { submit } from "./features/support/supportTickets";
