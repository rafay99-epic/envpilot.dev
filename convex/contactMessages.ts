/**
 * Compat barrel — preserves the public `api.contactMessages.*` paths.
 * Implementation lives in features/support/contactMessages.ts.
 */
export { submit } from "./features/support/contactMessages";
