.automaker
.vscode
.next
.turbo
out
build
node_modules
dist
convex/_generated
coverage
package-lock.json
bun.lock
*.vsix
**/routeTree.gen.ts
.agents
test-results
playwright-report
.convex/

# Multi-entry changelog. Frontmatter fences sit mid-file, where markdown
# reads `---` as a setext heading underline — Prettier rewrites every entry
# marker into a heading and destroys the frontmatter. Structure here is
# parsed by src/lib/changelog.ts, not styled.
apps/web/content/CHANGELOG.md
