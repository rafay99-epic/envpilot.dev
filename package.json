{
  "name": "env-connect-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --names web,convex --prefix-colors blue,green \"turbo dev --filter=@env-connect/web\" \"convex dev\"",
    "dev:web": "turbo dev --filter=@env-connect/web",
    "dev:convex": "convex dev",
    "dev:cli": "turbo dev --filter=@env-connect/cli",
    "dev:extension": "turbo dev --filter=env-connect",
    "build": "turbo build",
    "build:web": "turbo build --filter=@env-connect/web",
    "build:cli": "turbo build --filter=@env-connect/cli",
    "build:extension": "turbo build --filter=env-connect",
    "package:extension": "cd apps/vscode-extension && npx @vscode/vsce package --no-dependencies",
    "lint": "turbo lint",
    "lint:web": "turbo lint --filter=@env-connect/web",
    "lint:cli": "turbo lint --filter=@env-connect/cli",
    "lint:extension": "turbo lint --filter=env-connect",
    "typecheck": "turbo typecheck",
    "typecheck:web": "turbo typecheck --filter=@env-connect/web",
    "format:check": "prettier --check .",
    "format:fix": "prettier --write .",
    "test:e2e": "turbo test:e2e --filter=@env-connect/web",
    "test:cli": "turbo test --filter=@env-connect/cli",
    "test:extension": "turbo test --filter=env-connect",
    "check:all": "turbo lint typecheck build && prettier --check .",
    "convex:deploy": "convex deploy",
    "setup": "cp -n .env.example .env.local 2>/dev/null; ln -sf ../../.env.local apps/web/.env.local && echo 'Environment ready — edit .env.local with your values'",
    "generate:secret": "openssl rand -base64 32 | tr -d '\\n' && echo ''"
  },
  "devDependencies": {
    "@env-connect/prettier-config": "workspace:*",
    "concurrently": "^9.1.2",
    "convex": "^1.32.0",
    "prettier": "^3.6.2",
    "turbo": "^2"
  },
  "prettier": "@env-connect/prettier-config"
}
