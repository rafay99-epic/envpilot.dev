// Shared chrome for the AUTHENTICATED surfaces (dashboard + admin panel),
// as opposed to ./marketing (public site) and ./docs (docs site).
export { PageHeader, type PageHeaderProps } from "./PageHeader";
